.env.local
.env.production
.env.development
nodemodules

        // secure: !isLocalhost,           // ← ONLY secure in production
        // sameSite: isLocalhost ? 'lax' : 'none',  // ← lax for localhost